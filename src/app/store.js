import { configureStore } from '@reduxjs/toolkit'
// put reducers here
// ie  import counterReducer from '../components/counterSlice'

export default configureStore({
  reducer: {
    //reducers ie
    //counter: counterReducer
  }
})